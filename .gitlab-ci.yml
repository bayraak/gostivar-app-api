cache:
  paths:
    - node_modules/
    
stages:
  - development
  
deploy:
  type: deploy
  stage: development
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - build/
  only:
    - master